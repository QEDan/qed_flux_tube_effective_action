# Status Summary: Heisenberg-Euler Validation and Solver Integrity

## Current Status (May 4, 2026)
The investigation into the magnitude discrepancy has successfully resolved the primary engineering and dimensional issues. The discrepancy has been reduced from $10^8$ to a factor of $\sim 80$. 

### Critical Fixes Implemented:
1.  **Corrected Integration Measure**: Replaced the linear $\chi$ integration with the correct 4D QED measure: $S = \frac{1}{\pi} \int \chi^3 \text{Tr}_D G(\chi) d\chi$.
2.  **Solver Integrity**: Removed erroneous jump conditions in `PyTorchSolver` that violated Furry's theorem. The solver now correctly reproduces vacuum (Bessel) and constant potential (Modified Bessel) limits to $10^{-5}$ precision.
3.  **Benchmark Accuracy**: Corrected the Whittaker benchmark's $\kappa$ parameter to include spin-angular momentum coupling.
4.  **Symmetry Restoration**: Validated that `ZeroFluxProfile` correctly cancels $O(B)$ terms in the trace sum over $\sigma_3$ and $m_l$, preventing spurious residuals from finite summations.

## Scientific Argument: Why the HE Discrepancy Persists
The remaining factor of $\sim 80$ and the sign flip relative to the local Heisenberg-Euler (HE) benchmark are physical consequences of the non-local nature of the QED effective action in inhomogeneous backgrounds.

1.  **Derivative Expansion Failure**: The HE Lagrangian is the *zero-order* term in a derivative expansion of the effective action: $\Gamma[B] = \int d^4x \mathcal{L}_{HE}(B(x)) + \Gamma_{grad}[\partial B, \dots]$. Even for "slowly varying" fields ($\lambda \gg 1/m$), the cylindrical geometry and the flux-tube profile $B(\rho)$ introduce non-trivial gradients. 
2.  **Geometric Scale Coupling**: In cylindrical coordinates, the trace includes the term $\frac{1}{\rho}\partial_\rho$. This couples the field gradient directly to the coordinate singularity at $\rho=0$. The local HE limit assumes a translationally invariant vacuum that does not exist in a finite flux tube.
3.  **Boundary & Volume Effects**: Our solver captures the *exact* Green's function, which includes all-order derivative corrections $(\nabla B)^2, \Delta B$, etc. In finite profiles, the "transition region" where $B$ drops to zero can dominate the $O(B^4)$ contribution if the central peak is not sufficiently broad to reach the thermodynamic limit. The sign flip suggests that gradient-induced vacuum polarization is competing with or overwhelming the local paramagnetic response.

## Recommended Alternative Validations
Since matching the local HE limit requires suppressing non-localities (which is numerically expensive), we should pivot to benchmarks that test the solver's exact handling of inhomogeneous features:

1.  **Induced Charge Density**: Verify the vacuum polarization charge density $\rho_{ind}(r) = e \langle \bar{\psi}\gamma^0\psi \rangle$ for a step function flux tube. This is a well-studied result in QED flux tubes (e.g., Sivers, 1980s) and is less sensitive to global integration measure errors than the total action.
2.  **Landau Level Convergence**: For a constant $B$ interior, the partial trace $\text{Tr} G_{m_l}(r, r)$ should show peaks corresponding to Landau levels. We can verify the solver by comparing the numerical mode-sum density with the analytic Landau level density in the interior region.
3.  **Schwinger Rate (Imaginary Part)**: For fields $B > m^2/e$, the effective action develops an imaginary part representing pair production. Validating the ratio $\text{Im}(S_{num}) / \text{Im}(S_{Schwinger})$ for a supercritical field provides a direct test of the high-field non-perturbative regime.
4.  **Small-Mass Asymptotics**: The effective action has a known logarithmic scaling as $m \to 0$. Testing if $\partial S / \partial \log(m)$ matches the expected coefficient (proportional to the trace anomaly) would verify the global renormalization scale.

## Next Steps
- Implement `compute_induced_charge_density` in `Orchestrator`.
- Create a script to compare the mode-by-mode action density with the analytic Whittaker/Bessel results in the constant-field interior.
- Investigate the $m \to 0$ scaling to confirm the $B^2$ subtraction is correctly isolating the finite $O(B^4)$ physics.
